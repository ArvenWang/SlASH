{
  "name": "project-slash",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --host 127.0.0.1",
    "check": "tsc --noEmit",
    "test": "vitest run",
    "test:rules": "vitest run tests/redesign-skills-run.test.ts tests/redesign-gameplay.test.ts tests/redesign-path-presentation.test.ts tests/redesign-persistence-replay.test.ts",
    "verify:full-run": "vitest run tests/redesign-full-run.test.ts",
    "verify:residue": "node scripts/verify-production-residue.mjs",
    "verify:browser": "node scripts/verify-browser.mjs"
  },
  "dependencies": {
    "three": "0.185.1"
  },
  "devDependencies": {
    "@types/three": "0.185.4",
    "playwright": "1.62.1",
    "typescript": "7.0.2",
    "vite": "8.2.1",
    "vitest": "4.1.10"
  }
}
