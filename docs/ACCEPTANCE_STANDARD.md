# Project Slash — Phase 1 验收标准

文档版本：v1.3
适用范围：Phase 1 Visual Vertical Slice
状态：正式质量门槛，后续 Agent、Visual Critic、性能验收和 GitHub 交付统一以此为准。

## 1. 最终目标

Phase 1 不是普通玩法原型。最终交付必须是一段可以独立游玩的正式品质切片：

- 一张雨夜“交通大教堂”企业平台；
- 一名未来赛博剑士；
- 一种宽重型近战敌人；
- 三个固定 Stage；
- 唯一核心动作：点击目标点，沿直线高速突刺，并击杀路径上的全部敌人；
- Web 桌面版为主目标，1080p / 60 FPS；
- 游戏内容可以少，但画面内不能保留明显的 Blockout、Placeholder、假按钮、假动画或技术 Demo 质感。

一句话目标：

> 第一次看到真实运行画面的人，应认为这是一个范围很小但已经进入商业制作阶段的游戏，而不是一个 Three.js 练习场景。

## 2. 完成判定

必须同时满足以下条件才允许宣布 Phase 1 完成：

1. 所有“一票否决门”通过；
2. 总评分至少 `92 / 100`；
3. Gameplay、Character、Animation、VFX/Gore、Environment 五个核心分类均不得低于该分类满分的 `90%`；
4. 不存在 P0 或 P1 缺陷；
5. 独立 Visual Critic 不再给出“Blockout、技术 Demo、Asset Demo、角色不可读、特效廉价”中的任何一项结论；
6. 自动化、真实浏览器、生产构建、截图/录像、性能报告和许可证清单齐全；
7. 用户完成最终真人试玩与视觉确认。

代码能够编译、浏览器能够打开、Dash 能够移动，都只算基础工程完成，不等于 Phase 1 完成。

## 3. 一票否决门

以下任一项不通过，整体直接判定为未完成，不进入总分讨论：

| 编号 | 硬门 | 失败条件 |
|---|---|---|
| H-01 | 角色概念与正式化 | 主角或敌人没有先完成项目内三视图/动作基准；实时模型仍明显由基础方块、圆柱、胶囊拼成；或模型与锁定概念的轮廓、比例、配色、装备语言明显不一致 |
| H-02 | 动画闭环 | 缺少 Idle、Turn、Anticipation、Dash、Arrival、Recovery、Death 中任一关键动作，或仍以瞬移/旋转模型冒充动画 |
| H-03 | 击杀闭环 | 路径命中后没有延迟切口、身体分离、血液、火花/碎片和尸体落地的完整视觉链 |
| H-04 | 核心规则 | Dash 不是严格直线、会漏杀高速路径上的敌人、Dash 期间不无敌、Recovery/Input Buffer 不符合规则 |
| H-05 | 战场可读性 | 1080p 下看不清主角、敌我仅靠一个小红点区分、刀线被环境光或 Bloom 吞没 |
| H-06 | 场景层次 | 画面仍是平台浮在黑色背景中，或背景只是大片无细节方块，没有轨道、列车和三层城市纵深 |
| H-07 | 真实性 | 使用假数据、假按钮、预渲染画面冒充实时游戏，或截图内容无法在实际 Canvas 中复现 |
| H-08 | 稳定性 | 正常三关流程出现崩溃、卡死、黑屏、控制失效或未处理的控制台错误 |
| H-09 | 性能 | 1080p High 最坏场景无法达到性能门槛，或性能只在空场景下测试 |
| H-10 | 商业许可 | 任一第三方资产来源/许可不清楚，或包含从其他商业游戏提取的模型、贴图、动画、声音 |

## 4. 评分结构

每项使用 0–5 级评分：

- `0`：缺失；
- `1`：Blockout / Placeholder；
- `2`：功能存在，但明显粗糙；
- `3`：普通独立游戏可用；
- `4`：商业化且统一；
- `5`：可以作为宣传画面或核心卖点。

| 分类 | 权重 |
|---|---:|
| 核心游戏性与操作手感 | 12 |
| 镜头、灯光、环境与构图 | 13 |
| 主角与敌人造型 | 18 |
| 动作与动画 | 20 |
| Dash、击杀、血液与肢解特效 | 20 |
| 音效与镜头反馈 | 5 |
| Web 性能、兼容性与加载 | 7 |
| 商业完整度与交付 | 5 |
| 合计 | 100 |

## 5. 核心游戏性验收（12 分）

### G-01 输入响应

目标：点击后主观上立即响应。

- Pointer/Touch 输入到逻辑进入 Dash：不超过 1 个固定逻辑 Tick；
- 输入到首个可见反馈：不超过 1 个显示帧；
- 100 次连续点击测量中，视觉响应 P95 不超过 `33ms`；
- 不允许等待角色完整转身后才发动。

证据：浏览器 Performance Mark JSON、100 次输入统计、60 FPS 与慢放录像。

### G-02 Dash 规则

- 目标距离无人工上限，只受 Arena / Wall 限制；
- Transit 时间随距离变化并钳制在 `35–110ms`；
- Dash 全程无敌；
- 目标越界时钳制到合法边界，不能无响应；
- 路径必须是单一直线，禁止弧线、自动寻路或绕行。

证据：自动化边界测试、可视化 Debug 路径、实际浏览器录像。

### G-03 多杀与碰撞

- 使用 Swept Segment / Capsule，不依赖单帧碰撞；
- 同一直线上的 1、5、20 个敌人必须全部击杀；
- 60、120、144 FPS 下结果一致；
- 斩杀半径必须可调，且视觉刀线与逻辑命中区域一致。

证据：自动化测试、三种帧率回放、`render_game_to_text` 状态。

### G-04 Recovery 与 Input Buffer

- Recovery 默认 `120ms`，可调范围 `80–180ms`；
- Recovery 期间玩家可以死亡；
- 只保存最后一个有效输入，不形成长队列；
- Recovery 结束后，缓存输入立即执行。

### G-05 死亡与重开

- 非无敌期间敌人有效接触即死；
- 判定后立即产生死亡反馈；
- 从判定死亡到重新可操作应 `< 1 秒`；
- 不弹确认框，不出现额外 Loading；
- 点击允许跳过剩余死亡展示。

### G-06 三关流程

- Stage 01 / 02 / 03 分别为 8 / 12 / 18 名敌人；
- 同一地图，通过出生位置、节奏和速度提升压力；
- 杀光敌人即过关；
- 三关可以从头到尾完成，不需要 Debug 操作；
- 第三关应自然产生 3、5 和更高 Multi Kill 的机会。

## 6. 镜头、灯光、环境与构图验收（13 分）

### V-01 镜头构图

在 1920×1080、2560×1440、1366×768 三种桌面尺寸下：

- Arena 四条边完整可见，并保留安全留白；
- Gameplay Space 占画面面积约 `55–72%`；
- 远景城市占画面高度约 `22–35%`；
- 不是纯俯视，可以读出胸腔、腿部、持刀姿势和角色前后关系；
- 镜头不传统跟随玩家；
- Dash 时仅允许微量 Directional Kick、Zoom 和 Exposure，不能影响落点判断。

证据：三分辨率空场、战斗、Multi Kill 截图及叠加安全框。

### V-02 四层空间

画面必须同时存在：

1. Foreground：平台边框、轨道结构或近景设施；
2. Gameplay：清晰、低障碍的 Arena；
3. Midground：巨型拱架、列车、维护结构；
4. Background：至少三层高度与密度不同的城市，并受距离雾控制。

失败示例：大片无意义黑墙、重复方块城市、列车像玩具、背景亮度高于刀线。

### V-03 企业视觉语言

必须形成一致的重复规则，例如：

- 斜切角；
- 三道凹槽；
- 橙色安全灯；
- 统一编号与维护标识；
- 相同的金属/混凝土/发光材质体系。

不能依靠鸟居、灯笼、汉字拼贴或随机霓虹制造“赛博朋克”。

### V-04 湿地材质

- 地面不得像灰色方格测试板；
- 至少能读出干区、湿区、积水、排水槽、接缝和局部维修补丁；
- 积水 Roughness 目标 `0.08–0.15`，干区 `0.3–0.45`；
- 反射不能是均匀镜面；
- Slash、人物灯光和列车灯应在湿地上有方向正确的反应；
- 不能用随机黑色烟斑冒充湿润变化。

### V-05 色彩与亮度层级

- 环境：Graphite / Dark Blue / Low-saturation Cyan；
- 主角：冷白 / 青白；
- 敌人：受控红橙；
- 血液：高饱和真实红；
- 刀锋与 Slash Core 必须始终是画面最高亮度；
- 纯白过曝区域不得超过画面像素的 `1%`；
- 禁止紫蓝粉全部高饱和的 Neon Soup。

证据：原图、灰度图、亮度直方图、Bloom Mask。

## 7. 主角与敌人造型验收（18 分）

### C-00 概念锁定与建模一致性

正式建模前必须先锁定下列项目内概念基准。2026-08-10 已根据用户对“比例正确、连续人体、多面几何、前卫造型、姿势优先”的反馈正式修订为 V5。V4 及更早版本只保留为迭代记录，不再作为当前模型通过依据。

- `art/characters/concepts/hero-turnaround-v5.png`；
- `art/characters/concepts/enemy-turnaround-v5.png`；
- `art/characters/concepts/hero-action-sheet-v2.png`；
- `art/characters/concepts/enemy-action-cut-sheet-v2.png`。

概念与模型必须满足：

- 三视图为同一角色、同一服装、同一比例与同一武器，不允许用气氛图代替建模依据；
- 主角采用细长、前卫、黑色切面轮廓与冷白长刀；敌人采用宽重切面轮廓与红橙倒 V 胸肩识别块；
- 使用原创的低至中多边形大切面语言，不复制《师父》或任何现有游戏的角色、服装、头盔、武器和动画；
- 在正面、侧面、背面正交叠加检查中，先以头顶/足底和身体中心做统一缩放、平移注册；肩线、肩冠最大宽、最窄腰、胯点、膝点的左右活体轮廓边界，逐视图平均与最大偏差均不得超过 `5%` 身体高度。V5 必须使用重新标定的读点；数值工具不得把 Rig pivot 当作可见人体边界，也不得复用 V4 或更早版本的人工读点；
- 叠加图必须继续展示概念与模型的手持武器二维可视投影，但武器硬门只能使用真实世界长度：主角刀必须为身高的 `90–96%`，敌人短刀必须记录真实世界长度及其书面设计区间。禁止把下垂持刀的二维投影长度直接与真实长度相减；
- 模型可以为动画、远镜头可读性和 Web 性能做局部工程调整，但必须记录原因，不能借优化之名改回通用科幻士兵；
- 动作状态机的关键 Line of Action 必须能对应动作基准图，不能只让整个模型平移、缩放或瞬间旋转。

证据：概念版本记录、三视图模型叠加图、Turntable、64px 轮廓表、动作关键帧对照，以及分开列出的数值门、世界长度门与人工主观门。自动量测只能报告数值门，不得自行伪称主观美术门通过。

### C-00A V5 正式建模视觉门

V5 的角色通过必须同时满足三层检查，任何一层都不能由另一层代替：

1. **近景建模层**：1024×1024 Character Lab 的正面、侧面和 3/4 视图中，颈肩、胸腰、髋腿、肘腕、膝踝必须形成连续人体；不得出现暴露的圆形端盖、关节插头、悬浮甲片、积木鞋、棍状手臂或基础几何拼装感。
2. **动作轮廓层**：主角 Ready 必须双脚真实落地、骨盆下沉、胸肩前压、刀柄贴近腰侧；敌人必须是带重量的冲跑，而不是站立平移、整齐复制或身体接近水平的爬行姿态。
3. **真实战场层**：固定生产镜头中，主角必须能同时读出头、躯干、双腿和整片发光刀，不得只剩一团刀光；同屏至少 8 名敌人时，应直接看出不少于 4 种步相、肩线和武器高度组合。

主观失败词包括：`Blockout`、`积木人`、`胶囊人`、`玩具机器人`、`关节断裂`、`站桩`、`复制姿势`、`刀光吞人`。只要任一词仍适用于真实画面，H-01 或 H-05 就不能通过。

### C-01 主角 Hero Asset

- 最终必须为经过重制的正式模型，不接受基础几何体拼接；表面可以保持多面几何风格，但人体比例、关节过渡和剪影必须先成立；
- 高挑、窄腰、长腿，具有清晰肩线和头部识别点；
- 头部使用未来传感器/多目视觉语言，不复制现有 IP；
- 刀长约为身体高度的 `80–110%`；
- 黑色主体必须通过近黑柔性层、深石墨大甲面和克制的冷白能量三类材质建立层次，不允许用碎片装甲与随机饰线堆细节；
- 武器必须是约身高 90–96% 的极薄轻弯能量武士刀，整片刀身连续发光，不能恢复厚黑芯或只亮单侧刃；
- 发光面积应克制，不能把人物变成青白光球；
- 1080p Gameplay 中角色屏幕高度至少 `50px`；
- 在 64px 高的纯轮廓测试中仍能识别头、肩、腿和刀。

证据：前、后、侧、3/4 Turntable；材质拆解；64px Silhouette Sheet；Gameplay 截图。

### C-02 敌人正式模型

- 肩、胸、前臂明显比主角宽重；
- 在不看颜色时，仅靠轮廓也能与主角区分；
- 红橙识别块至少覆盖头、胸或肩部之一，不能只依赖一个红点；
- 造型必须支持清晰的身体切分和机械碎片；
- 不得保留普通现代枪兵或卡通机器人语义。
- 冲跑时胸腔总前倾应保持为明确冲锋姿态，不能因父子关节旋转叠加而接近水平；抬腿、落脚和刀臂必须仍能看出人体结构。

### C-03 Web 资产预算

目标范围，不是以牺牲效果为代价的绝对数字：

- Player LOD0：目标 `4k–12k triangles`；
- Enemy LOD0：目标 `3.5k–10k triangles`；18–20 人同屏时优先保留大轮廓和关节连续性；
- 三角面数量不是独立通过条件：不得通过无意义细分凑数，也不得以 Web 性能为由保留明显的 1k 级基础块状模型；
- 角色正式材质尽量控制在 2–3 套；
- 贴图使用 KTX2 / Basis，远镜头不保留无效 4K 细节；
- 必须提供 LOD 或距离降级策略；
- 所有第三方资产必须经过轮廓、材质、配色和比例重制，禁止原样拖入。

## 8. 动作与动画验收（20 分）

### A-01 主角动画集

至少包含：

- Combat Idle；
- Micro Turn；
- 5–25ms 的视觉 Anticipation；
- Dash Pose；
- Arrival / Landing；
- 80–180ms Recovery / Sheathe；
- Player Death。

要求：

- 动作必须来自同一套身体语言；
- 转向不能瞬间机械旋转；
- Anticipation 不能增加实际输入延迟；
- Dash 不是播放跑步动画；
- Recovery 看起来是收刀/稳定身体，而不是等待 CD；
- 动画切换不能出现明显 Pose Pop、穿模或武器脱手。

### A-02 敌人动画集

至少包含：

- Idle；
- Locomotion；
- Turn；
- Contact Attack / Threat Pose；
- Hit Freeze；
- Delayed Cut；
- Body Separation / Fragment Landing。

要求：

- 步幅与移动速度误差不超过 `10%`，不得滑步；
- 转身有平滑过渡和身体倾斜；
- 所有敌人不得机械地朝玩家平移；
- 同屏 20 名敌人允许动画相位错开，避免完全同步。

### A-03 时间线验收

目标时间线：

| 时间 | 事件 |
|---:|---|
| 0ms | 输入 |
| 0–25ms | 刀锋亮度 Spike / 微量压身 |
| 15–110ms | Dash Transit |
| 20–130ms | 3–4 层 Afterimage / Slash Core |
| 40–100ms | Enemy Cut Seam 出现 |
| 120–230ms | 身体分离 |
| 130–300ms | 血液、火花、碎片 |
| 80–180ms | Arrival / Recovery |
| 180–400ms | Trail 衰减 |

证据：正常速度录像、4× 慢放、逐帧时间标注。

## 9. Dash、击杀、血液与肢解验收（20 分）

### FX-01 Slash Line

- 必须是单一、极细、笔直的冷白 Core；
- 1080p Core 建议为 `1–3px`，外围 Cyan Glow 不超过约 `8px`；
- 不得变成粗激光柱、弧形剑气或闪电；
- 湿地必须捕捉方向正确的短暂反射；
- 视觉位置与真实命中段一致。

### FX-02 Afterimage

- 只使用 `3–4` 层；
- 从起点到落点按时间排列；
- 透明度和细节逐层降低；
- 不能像复制了四个完整角色；
- 正常 Dash 后约 `350ms` 内清理。

### FX-03 敌人死亡

必须包含并按顺序发生：

1. 玩家穿过；
2. 敌人短暂保持站立；
3. 切口/热边出现；
4. 身体向斩击方向错位；
5. 身体分离；
6. 真实红血、火花和机械碎片爆发；
7. 大块尸体落地并保留到本 Stage 结束。

不接受立即消失、缩放到零、统一爆炸、整个角色像积木随机散架。

### FX-04 血液

- Blood Spray 必须具有斩击方向；
- Blood Mist、较大液滴、地面 Decal 分层存在；
- Decal 形状不规则，不能是圆形或简单椭圆；
- 血池与尸体位置对应；
- 血液为真实高饱和红，但不能盖住整个战场；
- Decal/粒子/碎片使用对象池并有上限。

### FX-05 环境反应

Dash 附近至少实现三项：

- 雨线被方向力带开；
- 雾被短暂切开；
- 积水或湿地出现扰动/反射；
- 松散粒子沿 Dash 方向偏移；
- 局部灯光/曝光产生极短响应。

### FX-06 Multi Kill

- 1 Kill：普通层级；
- 3 Kill：增加音效层和微量镜头反馈；
- 5 Kill：增强 Slash 保持与 Exposure；
- 8+ Kill：允许 40–70ms Hero Moment；
- 不使用遮挡画面的 DOUBLE KILL / PENTA KILL 巨大文字。

## 10. 音效与镜头反馈验收（5 分）

- Rain、City、Blade Hum、Dash、Air Whoosh、Blade、Flesh、Metal、Blood、Death、Multi Kill 均有独立层；
- Dash 不能只播放一个 `whoosh.wav`；
- 输入到声音首个瞬态 `< 50ms`；
- 同类击杀至少提供 6 组微变化，避免连续重复；
- 多敌人同时死亡时不得削波或爆音，Master Peak 不超过 `-1 dBFS`；
- 镜头反馈不能影响方向判断或引起持续眩晕；
- 支持关闭声音，浏览器自动播放限制下不报错。

## 11. Web 性能、兼容性与加载验收（7 分）

### P-01 1080p High 硬目标

测试机基线：Apple M1 Pro 16-core GPU，当前 Chrome 稳定版。

Worst Case 场景持续 60 秒：

- 20 名敌人；
- 一次击杀 8 名；
- Rain、Steam、Blood、Fragments、Corpses、Camera FX、Post FX 同时存在；
- 平均 FPS `>= 59`；
- P95 Frame Time `<= 18.33ms`：即 60Hz 一帧 `16.67ms` 加 10% 浏览器调度容差；必须同时保存同机空白页 rAF 基线与游戏原始值，不允许平滑、截断或删除慢帧；
- P99 Frame Time `<= 24ms`；
- 除首次资产加载外，不允许单帧超过 `50ms`；
- 10 分钟运行后，内存不能持续无上限增长。

### P-02 分辨率与降级

- 2560×1440 High：平均 FPS `>= 55`；
- Compatibility 档必须保留同一 Art Direction；
- 允许降低粒子、阴影、反射和 Post FX；
- 不允许降级后变成无灯光、无材质的低质量 Demo；
- 触摸输入必须真实可用，移动端允许降低画质。

### P-03 加载

- 初始压缩下载 Soft Target `< 50MB`，Hard Target `< 80MB`；
- 冷加载必须有正式 Loading 画面和真实进度；
- 50Mbps 网络下进入可操作状态目标 `< 8 秒`；
- 不能在首次 Dash 时临时加载关键 VFX/音效造成卡顿。

### P-04 浏览器

至少验证：

- Chrome 最新稳定版；
- Edge 最新稳定版；
- Safari 最新稳定版；
- WebGL2 Compatibility 路径；
- 浏览器 Resize、Fullscreen、DPR 变化和后台恢复。

## 12. 商业完整度与交付验收（5 分）

- HUD 极简且无开发占位文案；
- 自定义 Cursor / Touch Indicator 正常；
- Debug Panel 默认隐藏，不能进入生产画面；
- 无默认浏览器错误样式、Broken Asset、测试贴图或 Console Error；
- `ASSET_LICENSES.md` 记录第三方资产名称、作者、链接、许可、修改和最终用途；
- CC BY 内容在游戏或随包文档中正确署名；
- 不包含密钥、账号信息、下载缓存和无关大文件；
- Production Build 可在全新环境运行；
- Git 工作树干净，提交范围与交付内容一致。

## 13. 强制证据包

最终交付必须在 `validation/` 中包含：

```text
validation/
  README.md
  automated/
    typecheck.txt
    build.txt
    gameplay-tests.txt
    browser-console.json
  screenshots/
    idle-1080p.png
    dash-1080p.png
    multi-kill-1080p.png
    death-1080p.png
    stage-clear-1080p.png
    compatibility.png
  video/
    full-run.webm
    dash-slow-motion.webm
    death-slow-motion.webm
  performance/
    high-1080p.json
    high-1440p.json
    compatibility-1080p.json
    memory-10min.json
  visual-critique/
    round-01.md
    final.md
  character/
    hero-orthographic-overlay.png
    enemy-orthographic-overlay.png
    hero-turntable.webm
    enemy-turntable.webm
    silhouette-64px.png
  licenses/
    asset-audit.txt
```

每个证据必须来自真实运行产物。概念图、源码截图和口头说明不能替代真实游戏证据。

## 14. 缺陷等级

| 等级 | 定义 | 例子 | 是否允许交付 |
|---|---|---|---|
| P0 | 游戏无法完成或数据/安全严重问题 | 黑屏、崩溃、三关无法完成、许可违规 | 不允许 |
| P1 | 核心体验或视觉目标失败 | 角色像 Blockout、Dash 漏杀、动画缺失、最坏场景严重掉帧 | 不允许 |
| P2 | 明显质量问题 | 个别穿模、局部材质不统一、次要浏览器降级错误 | 原则上不允许 |
| P3 | 轻微瑕疵 | 极端角度小问题、非核心细节偏差 | 可记录后交付 |

## 15. 独立评审流程

每个核心模块必须执行：

```text
实现
→ 实际运行
→ 截图 / 录像 / 性能记录
→ 独立 Critic 检查
→ 列出具体缺陷
→ 修复
→ 重新捕获证据
→ 对比前后
→ 直到通过
```

Independent Visual Critic 不负责该模块的主要实现，也不能以“考虑到是 Web”作为降低视觉标准的理由。

## 16. 当前基线审计（2026-08-10，V5 视觉阶段）

当前版本的真实状态：

| 分类 | 当前判断 | 当前证据 | 是否通过 |
|---|---|---|---|
| Gameplay | 核心规则、输入、多杀、Recovery/Input Buffer 与三关流程已有真实证据；按用户要求不再重复运行 | `validation/final-gameplay-flow/`、`validation/automated/gameplay/` | 已关闭，不回归重测 |
| Environment | Transit Cathedral、湿地、列车、巨拱和城市纵深已有通过画面；当前角色工作不再改动或重验 | `validation/environment/wet-v2/` | 已关闭，不回归重测 |
| Player | V5 三视图已锁定；实时模型为 4,316 triangles，已移除腕踝端盖、缩回头部比例，并保留低位 Ready 与整片发光薄刀；实现门已达到，不能代替用户主观确认 | `art/characters/concepts/hero-turnaround-v5.png`、`validation/character/v5-iteration-7/` | 实现门通过；主观门待用户 |
| Enemy | V5 三视图已锁定；实时模型为 4,264 triangles，连续胸腰四肢、楔形鞋和短阔刀已成立，过度横趴已改为冲跑 | `art/characters/concepts/enemy-turnaround-v5.png`、`validation/character/v5-iteration-6/` | 实现门通过；主观门待用户 |
| Animation | Ready、Dash、Arrival、Recovery、敌人四组连续相位和死亡时间线存在；V5 Dash 已收成刀线领先、双腿后拖的单一动势 | `src/characters/animation.ts`、`validation/character/v5-iteration-7/` | 实现门通过；主观门待用户 |
| VFX / Gore | 延迟切缝、分离、血液、尸体落地和环境反应已有通过证据；当前不回归重测 | `validation/kill-timeline-v38-gore-final/` | 已关闭，不回归重测 |
| Audio | 分层音频、变体、峰值与静音门已有通过证据；真人最终听感仍由用户决定，当前不回归重测 | `validation/audio-phase1/` | 自动门已关闭 |
| Performance | 1080p / 1440p 可见最坏场景已有通过报告；首轮长测数据保留，按用户要求不再重复稳定性长测 | `validation/performance/final-1080p-calibrated/`、`validation/performance/final-1440p/` | 已关闭，不回归重测 |
| Commercial | Production Build 与许可证说明存在；标准证据包、用户验收、正式提交和远端同步未完成 | `dist/`、`ASSET_LICENSES.md` | 不通过 |

当前不再用旧 V4 分数或自动叠加结果替代 V5 主观判断。当前唯一主动制作重点是 Player、Enemy 与其动作轮廓；这些项目通过后才进入用户视觉确认。

## 17. 执行顺序

必须按以下顺序收口：

1. 锁定主角/敌人三视图与动作基准；
2. 正式主角、敌人和环境资产管线；
3. Rig、动作集和动画状态机；
4. Gameplay 手感与完整三关；
5. Dash / Kill / Cut / Gore / Environment Reaction；
6. Lighting、Wet Material、Camera、Audio；
7. 性能、兼容性、加载与对象池；
8. 独立 Critic 循环；
9. 证据包、许可证和用户真人验收；
10. 最终提交并同步 GitHub。

不得通过继续增加关卡、技能、Boss、Roguelike 或 UI 来绕开前五项。

## 18. GitHub 同步门

目标远端：`https://github.com/ArvenWang/SlASH.git`

只有在以下条件全部满足后才允许执行最终同步：

- 本文所有硬门通过；
- 总分至少 92；
- `validation/` 证据包齐全；
- 用户完成最终验收；
- `npm run check`、`npm run build`、Gameplay/Browser/Performance 测试通过；
- `git diff --check` 通过；
- 工作树只包含本项目交付内容；
- `ASSET_LICENSES.md` 完整；
- 不含密钥、下载缓存和不应公开的第三方原始资产。

远端推送不是完成质量工作的替代品，而是最后一步。
